# config/common - common options for live-build(7)


# Set debconf(1) priority to use
LB_DEBCONF_PRIORITY="high"

# Set initramfs compression
LB_INITRAMFS_COMPRESSION="lzma"

# Set base name of the image
LB_IMAGE_NAME="bsum-kali"

# Set options to use with lzma
LZMA_OPTIONS="-5 -T $(( $(nproc) / 2 )) --verbose"

# Enable UTC timestamps
LB_UTC_TIME="true"

# live-build options

# Enable color
# If set here, overrides the command line option
_COLOR="true"

# Enable verbose
# If set here, overrides the command line option
_VERBOSE="true"
