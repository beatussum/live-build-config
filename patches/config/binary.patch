From: Mattéo Rossillol‑‑Laruelle <beatussum@protonmail.com>
Subject: override `config/binary`
Signed-off-by: Mattéo Rossillol‑‑Laruelle <beatussum@protonmail.com>
Description:
This patch overrides the `config/binary` file created by `./build.sh`

--- a/config/binary
+++ b/config/binary
@@ -26,7 +26,7 @@

 # $LB_CHECKSUMS: set checksums
 # (Default: md5)
-LB_CHECKSUMS="md5"
+LB_CHECKSUMS="sha512sum"

 # $LB_COMPRESSION: set compression
 # (Default: none)
@@ -55,7 +55,7 @@

 # $LB_DEBIAN_INSTALLER_GUI: toggle use of GUI debian-installer
 # (Default: true)
-LB_DEBIAN_INSTALLER_GUI="true"
+LB_DEBIAN_INSTALLER_GUI="false"

 # $LB_GRUB_SPLASH: set custom grub splash
 # (Default: empty)
@@ -63,7 +63,7 @@

 # $LB_HDD_LABEL: set hdd label
 # (Default: DEBIAN_LIVE)
-LB_HDD_LABEL="DEBIAN_LIVE"
+LB_HDD_LABEL="BSUM_KALI_LIVE"

 # $LB_HDD_SIZE: set hdd filesystem size
 # (Default: auto)
@@ -75,19 +75,19 @@

 # $LB_ISO_APPLICATION: set iso author
 # (Default: Kali Linux)
-LB_ISO_APPLICATION="Kali Linux"
+LB_ISO_APPLICATION="Beatussum"

 # $LB_ISO_PREPARER: set iso preparer
 # (Default: live-build $VERSION; https://salsa.debian.org/live-team/live-build)
-LB_ISO_PREPARER="live-build $VERSION; https://salsa.debian.org/live-team/live-build"
+LB_ISO_PREPARER="live-build @LB_VERSION@; https://github.com/beatussum/live-build-config/"

 # $LB_ISO_PUBLISHER: set iso publisher
 # (Default: Kali)
-LB_ISO_PUBLISHER="Kali"
+LB_ISO_PUBLISHER="Beatussum; https://github.com/beatussum/; beatussum@protonmail.com"

 # $LB_ISO_VOLUME: set iso volume (max 32 chars)
 # (Default: Kali Live)
-LB_ISO_VOLUME="Kali Live"
+LB_ISO_VOLUME="Beatussum's Kali Live"

 # $LB_JFFS2_ERASEBLOCK: set jffs2 eraseblock size
 # (Default: unset)
@@ -139,7 +139,7 @@

 # $LB_NET_TARBALL: set net tarball
 # (Default: true)
-LB_NET_TARBALL="true"
+LB_NET_TARBALL="false"

 # $LB_ONIE: set onie
 # (Default: false)
